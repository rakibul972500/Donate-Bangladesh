//common functionalities

function getInnerTextById(id) {
    const inText = parseFloat(document.getElementById(id).innerText);
    return inText;
  }
  
  function getValueById(id) {
    const inValue = parseFloat(document.getElementById(id).value);
    return inValue;
  }